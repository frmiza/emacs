#+TITLE: Editor - comportamento básico
#+PROPERTY: header-args:emacs-lisp :tangle ./editor.el :mkdirp yes :comments org :shebang ";;; -*- lexical-binding: t; -*-"

This file include global behave, UI setup (Indent, Lines, Cursor etc)

* Files

** Disable automatic backup/autosave
#+begin_src emacs-lisp
  (setq make-backup-files nil)
#+end_src

** Memory session
#+begin_src emacs-lisp
  (save-place-mode 1)
  (recentf-mode 1)
  (savehist-mode 1)
#+end_src

* Indent
#+begin_src emacs-lisp
  (setq-default indent-tabs-mode nil)
  (setq-default tab-width 4)             ; tabstop
  (setq-default standard-indent 4)       ; softtabstop / shiftwidth
  (setq-default electric-indent-mode 1)  ; autoindent
#+end_src

* Line numbers

Disable line numbers on .org files, terminal/shell mode

#+begin_src emacs-lisp
  (global-display-line-numbers-mode 1)
  (setq display-line-numbers-type 'relative)
  (setq column-number-mode 1)

  (dolist (mode '(org-mode-hook
                   term-mode-hook
                   shell-mode-hook
                   eshell-mode-hook
                   pdf-view-mode-hook))
    (add-hook mode (lambda () (display-line-numbers-mode 0))))
#+end_src

* Cursor

Highlight current line and visual broken line

#+begin_src emacs-lisp
  (setq-default cursor-type 'line)
  (global-hl-line-mode 1) 
  (global-visual-line-mode 1)
  (setq-default truncate-lines nil)
#+end_src

* Prompt
#+begin_src emacs-lisp
  (fset 'yes-or-no-p 'y-or-n-p)
  (electric-pair-mode 1)
#+end_src

* Font Size
#+begin_src emacs-lisp
  (global-set-key (kbd "<escape>") 'keyboard-escape-quit)
  (global-set-key (kbd "C-=") 'text-scale-increase)
  (global-set-key (kbd "C--") 'text-scale-decrease)
#+end_src
